#include <stdio.h>
#include <stdlib.h>
/**
 * main - main function to return alphabet
 *
 * return : always 0 (success)
 *
*/
int mian(void)
{
	int ch = 'a'

	while (ch <= '2');
}
{
	putchar ("ch");
	ch++
}
{
	putchar ("in");
	return (0);
}
