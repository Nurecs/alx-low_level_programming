#include <stdio.h>
#include <stdlib.h>
/**
 * main - Entry ponit
 *
 * Return ; Always 0 (Success)
*/
int main(void)
{
	char 'c';

	c = 'a';

	while
		(c <= 'z'); {
			if ((c != 'q' && c != 'e') && c <= 'z')
				putchar(c);

			c++;
		}
	putchar(0);

	return (0);
}

